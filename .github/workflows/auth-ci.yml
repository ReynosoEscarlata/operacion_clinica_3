name: CI - auth

on:
  push:
    branches: ['**']
    paths:
      - 'services/auth/**'
      - 'packages/authz/**'
      - 'packages/messaging/**'
      - 'packages/contracts/auth/**'
      - 'scripts/localstack-setup.mjs'
      # Contract test (Pact, PLAN.md Fase 4): ver nota equivalente en
      # doctors-ci.yml.
      - 'pacts/gateway-auth.json'
      - 'tsconfig.base.json'
      - '.github/workflows/auth-ci.yml'
  pull_request:
    branches: ['**']
    paths:
      - 'services/auth/**'
      - 'packages/authz/**'
      - 'packages/messaging/**'
      - 'packages/contracts/auth/**'
      - 'scripts/localstack-setup.mjs'
      # Contract test (Pact, PLAN.md Fase 4): ver nota equivalente en
      # doctors-ci.yml.
      - 'pacts/gateway-auth.json'
      - 'tsconfig.base.json'
      - '.github/workflows/auth-ci.yml'

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: auth
          POSTGRES_PASSWORD: auth
          POSTGRES_DB: auth_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U auth"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

      # Fase 3b (ADR-014): emula SNS -- reemplaza la env var REDIS_URL
      # vestigial que tenía este workflow (Auth nunca usó Redis realmente).
      localstack:
        image: localstack/localstack:3
        ports:
          - 4566:4566
        env:
          SERVICES: sns,sqs,scheduler
        options: >-
          --health-cmd "curl -f http://localhost:4566/_localstack/health"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      NODE_ENV: test
      PORT: 4001
      LOG_LEVEL: silent
      DATABASE_URL: postgresql://auth:auth@localhost:5432/auth_db?schema=public
      DATABASE_URL_APP: postgresql://app_role:app_role_dev_password@localhost:5432/auth_db?schema=public
      AWS_REGION: us-east-1
      AWS_ENDPOINT_URL: http://localhost:4566
      AWS_ACCESS_KEY_ID: test
      AWS_SECRET_ACCESS_KEY: test
      DOMAIN_EVENTS_TOPIC_ARN: arn:aws:sns:us-east-1:000000000000:clinica-test-domain-events

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Workspace: @clinica/authz y @clinica/messaging no están publicados,
      # hay que instalar desde la raíz (ver build: convertir el repo en npm
      # workspaces).
      - name: Install dependencies (workspace root)
        run: npm install --workspace=services/auth --workspace=packages/authz --workspace=packages/messaging --include-workspace-root

      - name: Build @clinica/authz
        run: npm run build --workspace=packages/authz

      - name: Build @clinica/messaging
        run: npm run build --workspace=packages/messaging

      - name: Install scripts dependencies
        working-directory: scripts
        run: npm install

      - name: Provision LocalStack (topic/colas/DLQs/schedule-group)
        working-directory: scripts
        run: npm run localstack:setup

      - name: Generate Prisma Client
        working-directory: services/auth
        run: npm run prisma:generate

      - name: Lint
        working-directory: services/auth
        run: npm run lint

      - name: Apply migrations
        working-directory: services/auth
        run: npx prisma migrate deploy

      - name: Typecheck
        working-directory: services/auth
        run: npm run typecheck

      - name: Test
        working-directory: services/auth
        run: npm run test
